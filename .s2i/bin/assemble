#!/bin/sh
# Called during the image build life cycle.

CONFIG="/tmp/src"

if [ -d $CONFIG ]; then
    echo "Copying config files..."
    cp -rfv $CONFIG/config/* $AMQ_HOME/conf/
    
    echo "Done"
fi
